# cypress-tsconfig-path-demo

This repo demonstrates what actual `tsconfig.json` file is used by Cypress and `ts-node` when both tests and project itself are written in TypeScript.

## The issue details

TBA

## License

MIT
